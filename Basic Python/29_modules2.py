#Function import
from math import cos,sin  #imports two functions from math module ie., cos and sin

print(cos(90))          #can directly call the function
print(sqrt(16))         #throws an error, as sqrt function is not imported
